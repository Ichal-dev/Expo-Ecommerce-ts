import React from "react";
import { Text, SafeAreaView } from "react-native";

const Btn = () => {
  return (
    <SafeAreaView>
      <Text>Hello Button</Text>
    </SafeAreaView>
  );
};

export default Btn;
